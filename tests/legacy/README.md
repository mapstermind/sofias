# Legacy tests

These tests are retained temporarily because they existed before the repository adopted spec-driven and test-driven development.

They may contain coverage-only or implementation-coupled tests.

Do not add new tests here.

Before deleting a legacy test, replace its useful behavior with a behavior-focused or characterization test.
